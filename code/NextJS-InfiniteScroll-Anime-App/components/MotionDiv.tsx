"use client";

import { motion } from "framer-motion";

// Now the only thing that is client side rendered is the motion.div
export const MotionDiv = motion.div;
//# sourceMappingURL=MotionDiv.js.map
